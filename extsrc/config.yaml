schema: 1
name: my-develop
version: "0.1.4"
hints:
  global:
    - "Chat: Russian. Specs, comments, logs, strings in code: English. ASCII in code/logs/docs when practical."
    - "No emojis or exotic Unicode in code, logs, documentation, or messages; plain ASCII where practical."
    - "If the user only asked a question, answer first; do not edit files unless changes are clearly needed."
folders:
  spec/extend:
    mode: static
files:
  spec/extend/00-general.md:
    description: "Docs/comments/logs policy, builds, change recap."
    mode: static
    globalRead: required
    localRead: no
  spec/extend/01-csharp-style.md:
    description: "Fluent, DI, CPM, errors, C# for automation."
    mode: static
    globalRead: auto
    localRead: no
  spec/extend/02-csharp-solution-layout.md:
    description: "Layout, registrars, DI."
    mode: static
    globalRead: auto
    localRead: no
  spec/extend/03-entity-framework-core.md:
    description: "EF Core migrations (dotnet ef)."
    mode: static
    globalRead: auto
    localRead: no
  spec/extend/04-http-httclient.md:
    description: "IHttpClientFactory / HttpClient."
    mode: static
    globalRead: auto
    localRead: no
  spec/extend/05-tools-mcp.md:
    description: "Context7 MCP."
    mode: static
    globalRead: required
    localRead: no
skills: {}
setup: {}
agent-ignore: []
git-ignore: []
